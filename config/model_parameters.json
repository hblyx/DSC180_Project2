{
    "cora": {
        "default": {
            "optimizer": "Adam",
            "criterion": "CrossEntropyLoss",
            "lr": 0.001,
            "weight_decay": 0.0005,
            "epochs": 100
        }
    }, 

    "twitch": {
        "default": {
            "optimizer": "Adam",
            "criterion": "CrossEntropyLoss",
            "lr": 0.001,
            "weight_decay": 0.0005,
            "epochs": 100
        }
    }
}